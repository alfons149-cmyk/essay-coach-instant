{
  "name": "essaycoach-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "mobile:build": "node scripts/build-mobile.mjs",
    "mobile:sync": "npm run mobile:build && cap sync",
    "android:add": "npm run mobile:build && cap add android",
    "ios:add": "npm run mobile:build && cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  }
}
